apiVersion: batch/v1
kind: CronJob
metadata:
  name: pyspark-cronjob
spec:
  schedule: "0 * * * *"
  suspend: true
  jobTemplate:
    spec:
      backoffLimit: 0
      template:
        spec:
          containers:
            - name: pyspark-container
              image: pyspark-code-image:latest
              imagePullPolicy: Never
          restartPolicy: Never
