apiVersion: batch/v1
kind: CronJob
metadata:
  name: batch-pipelines-cronjob
spec:
  schedule: "0 * * * *"
  suspend: true
  jobTemplate:
    spec:
      backoffLimit: 0
      template:
        spec:
          containers:
            - name: pyspark-container
              image: pipelines-batch-image:latest
              imagePullPolicy: Never
          restartPolicy: Never
